%h1 Listing items

%table
  %tr
    %th Name
    %th Cost
    %th Restaraunt
    %th
    %th
    %th

  - @items.each do |item|
    %tr
      %td= item.name
      %td= item.cost
      %td= link_to item.restaurant.name, item.restaurant
      %td= link_to 'Show', restaurant_item_path(@restaurant, item)
      %td= link_to 'Edit', edit_restaurant_item_path(@restaurant, item)
      %td= link_to 'Destroy', restaurant_item_path(@restaurant, item), :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New item', new_restaurant_item_path(@restaurant)
